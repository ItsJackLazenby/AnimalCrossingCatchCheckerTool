import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.animalcrossing',
  appName: 'Animal Crossing Catch Checker',
  webDir: 'dist/animal-crossing-catch-checker-tool/browser'
};

export default config;
